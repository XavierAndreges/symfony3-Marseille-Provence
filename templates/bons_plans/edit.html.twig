<!DOCTYPE html>

<title>Edit BonsPlans</title>

{% block body %}
    <h1>Edit BonsPlans</h1>

    {{ include('bons_plans/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('bons_plans_index') }}">back to list</a>

    {{ include('bons_plans/_delete_form.html.twig') }}
{% endblock %}
